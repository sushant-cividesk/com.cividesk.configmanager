#!/usr/bin/env bash
set -euo pipefail
script_dir="$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)"
cmd="$(basename -- "$0")"
if [[ "$cmd" == "cvcfg" ]]; then cmd="civicfg"; fi
exec "$script_dir/civicfg" "$cmd" "$@"
